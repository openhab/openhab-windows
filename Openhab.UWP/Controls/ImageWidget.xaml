<local:WidgetBase
    x:Class="Openhab.UWP.Controls.ImageWidget"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Openhab.UWP.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid Margin="8, 4, 8, 4" Background="{StaticResource OpenHABLightGreyBrush}">
        <local:ImageLabel IconPath="{x:Bind Widget.IconPath, Mode=OneWay}" LabelText="{x:Bind Widget.Label, Mode=OneWay}" />
        <Image x:Name="CameraImage" Width="200" Height="200" Stretch="UniformToFill"
                HorizontalAlignment="Right"
                Source="{x:Bind CameraBitmapImage, Mode=OneWay}" />
    </Grid>
</local:WidgetBase>
