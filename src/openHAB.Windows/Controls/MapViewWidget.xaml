<local:WidgetBase x:Class="openHAB.Windows.Controls.MapViewWidget"
                  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                  xmlns:local="using:openHAB.Windows.Controls"
                  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                  xmlns:maps="using:Mapsui.UI.WinUI"
                  mc:Ignorable="d"
                  d:DesignHeight="300"
                  d:DesignWidth="400"
                  Visibility="{x:Bind Widget.Visibility}">

    <Grid Style="{StaticResource Widget}"
          Tapped="OnTapped">
        <maps:MapControl x:Name="MapView"
                         Tapped="OnTapped" />

        <ContentDialog x:Name="PopupDialog"
                       Title="{x:Bind Widget.Label}"
                       Background="{StaticResource OpenHABLightColor}"
                       IsPrimaryButtonEnabled="True"
                       PrimaryButtonText="Close"
                       DefaultButton="Primary">
            <maps:MapControl x:Name="MapViewFull"
                             Width="650"
                             Height="400" />
        </ContentDialog>
    </Grid>
</local:WidgetBase> 